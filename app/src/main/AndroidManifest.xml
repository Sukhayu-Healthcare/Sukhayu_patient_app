<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 Network + basic permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/asha_jyoti_app_logo"
        android:label="@string/app_name"
        android:theme="@style/Theme.RoleBasedTelemedicinePatient"
        android:supportsRtl="true">

        <!-- 🏠 Login: Launcher activity -->
        <activity android:name="com.sukhayu.patient.ui.login.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity> 



        <!-- 🩺 Core Patient Screens -->
        <activity android:name=".ui.dashboard.DashboardActivity" android:exported="true" />
        <activity android:name=".ui.profile.ProfileActivity" android:exported="true" />
        <activity android:name=".ui.consultation.ConsultDoctorActivity" android:exported="true" />
        <activity android:name=".ui.consultation.PastConsultationsActivity" android:exported="true" />
        <activity android:name=".ui.consultation.MedicinesActivity" android:exported="true" />
        <activity android:name=".ui.consultation.DoctorDetailActivity" android:exported="false" />

        <!-- 💬 AI Symptom Checker -->
        <activity android:name=".ui.ai_symptom.SymptomChatActivity" android:exported="true" />
        <activity android:name=".ui.ai_symptom.CheckSymptomsActivity" android:exported="true"/>

        <!-- 💊 Prescription / Consultation -->
        <activity android:name=".ui.consultation.PrescriptionActivity" android:exported="true" />

        <!-- 🦠 Health Awareness -->
        <activity android:name=".ui.awareness.DiseaseOutbreakActivity" android:exported="true" />

        
    </application>

</manifest>
