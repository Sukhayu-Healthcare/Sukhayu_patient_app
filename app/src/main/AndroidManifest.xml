<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 Network + basic permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RoleBasedTelemedicinePatient"
        android:supportsRtl="true">

        <!-- 🏠 Login: Launcher activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🩺 Core Patient Screens -->
        <activity android:name=".ui.dashboard.DashboardActivity" android:exported="true" />
        <activity android:name=".ProfileActivity" android:exported="true" />
        <activity android:name=".ConsultDoctorActivity" android:exported="true" />
        <activity android:name=".PastConsultationsActivity" android:exported="true" />
        <activity android:name=".MedicinesActivity" android:exported="true" />
        <activity android:name=".DoctorDetailActivity" android:exported="true" />

        <!-- 💬 AI Symptom Checker -->
        <activity android:name=".ui.ai_symptom.SymptomChatActivity" android:exported="true" />
        <activity android:name=".ui.ai_symptom.CheckSymptomsActivity" android:exported="true"/>

        <!-- 💊 Prescription / Consultation -->
        <activity android:name=".ui.consultation.PrescriptionActivity" android:exported="true" />

        <!-- 🦠 Health Awareness -->
        <activity android:name=".ui.awareness.DiseaseOutbreakActivity" android:exported="true" />

        <!-- ⚙️ Commented out backend-related components -->
        <!--
        <activity android:name=".ui.teleconsult.ConsentActivity" android:exported="true" />
        <activity android:name=".ui.teleconsult.VideoCallActivity" android:exported="true" />
        <activity android:name=".ui.teleconsult.VoiceCallActivity" android:exported="true" />
        <activity android:name=".ui.teleconsult.ChatFallbackActivity" android:exported="true" />
        <activity android:name=".ui.emergency.EmergencyInitActivity" android:exported="true" />
        <activity android:name=".ui.emergency.EmergencyVCActivity" android:exported="true" />

        <service android:name=".notification.CallNotificationManager" android:exported="false" />
        <receiver android:name=".notification.CallNotificationReceiver" android:exported="false" />
        <service android:name=".data.remote.SocketManager" android:exported="false" />
        -->
    </application>

</manifest>
