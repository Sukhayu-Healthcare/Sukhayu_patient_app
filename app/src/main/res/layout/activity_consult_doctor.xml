<?xml version="1.0" encoding="utf-8"?>
<!-- Consult Doctor screen: header with back button + start card -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:background="@android:color/white">

    <!-- Header: Back button + Title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="12dp">

        <!-- BACK button used by your Kotlin code (id = backButton) -->
        <ImageView
            android:id="@+id/backButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="8dp"
            android:src="@drawable/ic_arrow_back_24"
            android:scaleType="centerInside" />

        <TextView
            android:id="@+id/tvHeader"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="@string/consult_doctor"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingStart="8dp"
            android:textColor="#111"/>

    </LinearLayout>

    <!-- Keep your existing CardView - keeps the same id cardStart -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardStart"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Start Questionnaire"
            android:padding="16dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center" />
    </androidx.cardview.widget.CardView>

</LinearLayout>
